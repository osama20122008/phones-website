# دليل البدء السريع

## 🚀 ابدأ الآن

### 1. التثبيت
```bash
# استنساخ المستودع
git clone <repository-url>
cd phones_website

# تثبيت المكتبات
npm install
```

### 2. تشغيل خادم التطوير
```bash
npm run dev
```

ستظهر رسالة مثل:
```
  VITE v5.4.21  ready in 259 ms
  ➜  Local:   http://localhost:3001/
```

افتح المتصفح وانتقل إلى `http://localhost:3001/`

### 3. البناء للإنتاج
```bash
npm run build
```

ستجد الملفات المجمعة في مجلد `dist/`

---

## 📁 هيكل المشروع

```
phones_website/
├── src/
│   ├── components/           # مكونات React
│   │   ├── Header.tsx
│   │   └── PhoneCard.tsx
│   ├── pages/                # الصفحات
│   │   ├── Home.tsx
│   │   ├── AllPhones.tsx
│   │   ├── PhoneDetail.tsx
│   │   ├── Comparison.tsx
│   │   ├── Favorites.tsx
│   │   └── Settings.tsx
│   ├── services/             # خدمات البيانات
│   │   └── phoneService.ts
│   ├── store/                # إدارة الحالة
│   │   └── phoneStore.ts
│   ├── types/                # أنواع TypeScript
│   │   └── phone.ts
│   ├── data/                 # بيانات الهواتف
│   │   └── phones.json       # 7000 هاتف
│   ├── App.tsx               # المكون الرئيسي
│   ├── main.tsx              # نقطة الدخول
│   └── index.css             # الأنماط
├── public/                   # الملفات الثابتة
├── scripts/                  # السكريبتات
│   └── generate_phones_data.py
├── dist/                     # المخرجات (بعد البناء)
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── README.md
```

---

## 🎯 الميزات الرئيسية

### 🔍 البحث والتصفية
- ابحث عن أي هاتف بالاسم أو الماركة
- صفّ حسب السعر والفئة والتقييم

### 📊 المقارنة
- قارن حتى 4 هواتف جنباً إلى جنب
- شاهد جميع المواصفات في جدول واحد

### ❤️ المفضلة
- احفظ هواتفك المفضلة
- عد إليها بسهولة

### 💰 تحويل العملات
- جنيه مصري (ج.م)
- دولار أمريكي ($)
- ريال سعودي (ر.س)
- درهم إماراتي (د.إ)

---

## 📱 الصفحات الرئيسية

### الصفحة الرئيسية `/`
- أحدث الهواتف
- الهواتف الأفضل تقييماً
- أفضل الكاميرات والأداء
- إحصائيات عامة

### جميع الهواتف `/phones`
- عرض جميع 7000 هاتف
- بحث متقدم وتصفية
- ترتيب حسب معايير مختلفة

### تفاصيل الهاتف `/phone/:id`
- مواصفات كاملة
- صور وألوان
- أسعار من متاجر
- تقييمات شاملة
- هواتف ذات صلة

### المقارنة `/comparison`
- مقارنة حتى 4 هواتف
- جدول مقارنة شامل

### المفضلة `/favorites`
- عرض الهواتف المحفوظة
- إدارة المفضلة

### الإعدادات `/settings`
- اختيار العملة
- اختيار اللغة
- معلومات الموقع

---

## 🛠️ الأوامر المتاحة

```bash
# بدء خادم التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة الإنتاج محلياً
npm run preview

# فحص الأخطاء
npm run check

# تنسيق الكود
npm run format

# تشغيل الاختبارات
npm run test
```

---

## 🎨 التخصيص

### تغيير الألوان
عدّل `tailwind.config.js`:
```js
theme: {
  colors: {
    primary: '#0a7ea4',
    // ... الألوان الأخرى
  }
}
```

### تغيير البيانات
عدّل `src/data/phones.json` أو أعد توليد البيانات:
```bash
python3 scripts/generate_phones_data.py
```

### إضافة صفحات جديدة
أنشئ ملف جديد في `src/pages/` وأضفه إلى التوجيه.

---

## 📊 البيانات

### حجم البيانات
- **7000 هاتف** بمواصفات كاملة
- **28 MB** حجم ملف JSON
- **30+ ماركة** عالمية

### المواصفات المتضمنة
- معلومات أساسية
- الشاشة والمعالج
- الكاميرا والبطارية
- الأسعار والتقييمات
- المتاجر والروابط

---

## 🚀 النشر

### Vercel (الأسهل)
```bash
npm install -g vercel
vercel
```

### Netlify
```bash
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

### خادمك الخاص
```bash
npm run build
# انسخ مجلد dist/ إلى خادمك
```

---

## 🐛 استكشاف الأخطاء

### الخادم لا يبدأ
```bash
# تأكد من أن المنفذ 3001 متاح
lsof -i :3001
# أو استخدم منفذ مختلف
npm run dev -- --port 3002
```

### الملفات الثابتة لا تحمل
```bash
# امسح ذاكرة التخزين المؤقت
rm -rf node_modules/.vite
npm run dev
```

### بطء التحميل
- استخدم `npm run build` للإنتاج
- استخدم CDN للملفات الثابتة
- قلل حجم الصور

---

## 📚 المراجع

- [توثيق Vite](https://vitejs.dev/)
- [توثيق React](https://react.dev/)
- [توثيق Tailwind CSS](https://tailwindcss.com/)
- [توثيق Zustand](https://github.com/pmndrs/zustand)

---

## 💡 نصائح

1. **استخدم React DevTools** لتصحيح الأخطاء
2. **استخدم Tailwind CSS IntelliSense** لتسريع الكتابة
3. **استخدم Git** لحفظ التغييرات
4. **اختبر على أجهزة مختلفة** قبل النشر
5. **استخدم Google PageSpeed Insights** لتحسين الأداء

---

## ❓ أسئلة شائعة

**س: كيف أضيف هاتف جديد؟**
ج: عدّل `src/data/phones.json` أو أعد توليد البيانات بـ `python3 scripts/generate_phones_data.py`

**س: كيف أغير اللغة الافتراضية؟**
ج: عدّل `usePhoneStore` في `src/store/phoneStore.ts`

**س: كيف أنشر الموقع؟**
ج: اتبع تعليمات النشر في `DEPLOYMENT.md`

**س: هل يمكنني استخدام قاعدة بيانات حقيقية؟**
ج: نعم، عدّل `phoneService.ts` للاتصال بـ API أو قاعدة بيانات

---

## 📞 الدعم

للمساعدة والاستفسارات:
- البريد الإلكتروني: info@phones.com
- الهاتف: +966 50 000 0000

---

**استمتع بتطوير موقعك! 🎉**
