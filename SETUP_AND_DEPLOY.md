# 🚀 دليل الإعداد والنشر الكامل

## المرحلة 1: الإعداد المحلي

### 1.1 المتطلبات
- Node.js 16+ ([تحميل](https://nodejs.org/))
- npm 8+ أو yarn
- Git
- محرر أكواد (VS Code موصى به)

### 1.2 استنساخ المستودع
```bash
git clone <repository-url>
cd phones_website
```

### 1.3 تثبيت المكتبات
```bash
# تثبيت مكتبات Frontend
npm install

# تثبيت مكتبات Backend
cd server
npm install
cd ..
```

### 1.4 إعداد متغيرات البيئة
```bash
# نسخ ملف البيئة
cp .env.example .env.local

# تعديل .env.local بمعلومات قاعدة البيانات المحلية
VITE_API_URL=http://localhost:3000
DATABASE_URL=postgresql://user:password@localhost:5432/phones_db
```

### 1.5 بدء خادم التطوير
```bash
npm run dev
```

سيتم فتح الموقع على:
- Frontend: http://localhost:5173
- Backend: http://localhost:3000

---

## المرحلة 2: إعداد قاعدة البيانات

### 2.1 إنشاء قاعدة بيانات محلية

#### على macOS/Linux:
```bash
# تثبيت PostgreSQL
brew install postgresql

# بدء الخدمة
brew services start postgresql

# إنشاء قاعدة بيانات
createdb phones_db

# تشغيل schema
psql phones_db -f server/schema.sql
```

#### على Windows:
1. تحميل PostgreSQL من [postgresql.org](https://www.postgresql.org/download/windows/)
2. تثبيت مع الخيارات الافتراضية
3. فتح pgAdmin وإنشاء قاعدة بيانات جديدة
4. تشغيل schema.sql

### 2.2 التحقق من الاتصال
```bash
psql -U postgres -d phones_db -c "SELECT 1"
```

---

## المرحلة 3: النشر على Vercel

### 3.1 إنشاء حساب Vercel
1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط "Sign Up"
3. اختر "Continue with GitHub"
4. وافق على الأذونات

### 3.2 ربط المستودع
1. في لوحة تحكم Vercel، اضغط "New Project"
2. اختر مستودعك من GitHub
3. اضغط "Import"

### 3.3 إضافة متغيرات البيئة
في صفحة الإعدادات، أضف:

| المتغير | القيمة |
|--------|--------|
| `VITE_API_URL` | `https://your-domain.vercel.app` |
| `DATABASE_URL` | رابط Vercel Postgres |

### 3.4 إعداد قاعدة البيانات على Vercel

#### الخيار 1: Vercel Postgres (الموصى به)
1. في Vercel، اذهب إلى "Storage"
2. اضغط "Create Database"
3. اختر "Postgres"
4. اتبع الخطوات
5. سيتم إضافة `DATABASE_URL` تلقائياً

#### الخيار 2: قاعدة بيانات خارجية
استخدم أي مزود:
- [Supabase](https://supabase.com)
- [Railway](https://railway.app)
- [Render](https://render.com)

### 3.5 النشر
1. اضغط "Deploy"
2. انتظر انتهاء النشر (عادة 2-3 دقائق)
3. سيحصل على رابط مثل: `https://phones-website.vercel.app`

---

## المرحلة 4: إعداد النطاق (اختياري)

### 4.1 شراء نطاق
#### من Vercel:
1. اذهب إلى "Settings" > "Domains"
2. اضغط "Add Domain"
3. ابحث عن النطاق المتاح
4. اضغط "Buy"

#### من مزود آخر:
- GoDaddy
- Namecheap
- Google Domains

### 4.2 ربط النطاق
1. في Vercel، اذهب إلى "Settings" > "Domains"
2. أضف نطاقك
3. اتبع التعليمات لتحديث DNS

---

## المرحلة 5: الاختبار والتحقق

### 5.1 اختبار الموقع
```bash
# اختبار محلي
npm run dev

# بناء الإنتاج
npm run build

# معاينة الإنتاج
npm run preview
```

### 5.2 التحقق من الأداء
- استخدم [Google PageSpeed Insights](https://pagespeed.web.dev/)
- استخدم [GTmetrix](https://gtmetrix.com/)
- استخدم [WebPageTest](https://www.webpagetest.org/)

### 5.3 اختبار الوظائف
- البحث يعمل
- المقارنة تعمل
- المفضلة تُحفظ
- التقييمات تُضاف
- الأسعار تظهر
- الروابط تعمل

---

## المرحلة 6: الصيانة والتحديثات

### 6.1 تحديث البيانات
```bash
# توليد بيانات جديدة
python3 scripts/generate_phones_data.py

# نسخ البيانات
cp src/data/phones.json server/data/phones.json
```

### 6.2 تحديث المكتبات
```bash
# فحص التحديثات
npm outdated

# تحديث المكتبات
npm update

# تحديث مكتبات معينة
npm install package-name@latest
```

### 6.3 النسخ الاحتياطية
```bash
# نسخ احتياطية من قاعدة البيانات
pg_dump phones_db > backup.sql

# استعادة النسخة الاحتياطية
psql phones_db < backup.sql
```

---

## 🐛 استكشاف الأخطاء الشائعة

### مشكلة: خطأ في البناء
**الحل:**
```bash
# امسح node_modules
rm -rf node_modules
npm install

# امسح ذاكرة التخزين المؤقت
npm cache clean --force
npm install
```

### مشكلة: قاعدة البيانات لا تتصل
**الحل:**
```bash
# تحقق من DATABASE_URL
echo $DATABASE_URL

# اختبر الاتصال
psql $DATABASE_URL -c "SELECT 1"
```

### مشكلة: الموقع بطيء
**الحل:**
- استخدم CDN (Cloudflare)
- قلل حجم الصور
- استخدم lazy loading
- قلل حجم الـ bundle

### مشكلة: الموقع لا يحمل
**الحل:**
- تحقق من الأخطاء في المتصفح (F12)
- تحقق من سجلات Vercel
- تحقق من متغيرات البيئة

---

## 📊 قائمة التحقق النهائية

### قبل النشر
- جميع الاختبارات تمر
- لا توجد أخطاء في وحدة التحكم
- الموقع يعمل محلياً
- البيانات محدثة
- متغيرات البيئة صحيحة

### بعد النشر
- الموقع متاح على الإنترنت
- جميع الصفحات تحمل
- البحث يعمل
- المقارنة تعمل
- المفضلة تُحفظ
- الأسعار تظهر
- الروابط تعمل
- الأداء جيد (Lighthouse > 80)

---

## 📞 الدعم والمساعدة

### موارد مفيدة
- [توثيق Vercel](https://vercel.com/docs)
- [توثيق React](https://react.dev/)
- [توثيق PostgreSQL](https://www.postgresql.org/docs/)
- [توثيق Tailwind CSS](https://tailwindcss.com/docs)

### الدعم الفني
- البريد الإلكتروني: support@phones.com
- الهاتف: +966 50 000 0000

---

**تم إعداد الموقع بنجاح! 🎉**
